
[//]: # (Do not edit directly)
[//]: # (Generated by the Cocokits TypeDoc custom theme on Sun Feb 09 2025)

import { Meta } from '@storybook/addon-docs';
import { MdxPage } from '@cocokits/storybook-addon-theme';

export const tocItems = [{"id":"selection_lazypromise","name":"lazyPromise()"}];

<Meta title='Utils/Promise'/>
<MdxPage breadcrumb='Utils' title='Promise' tocItems={tocItems} hideThemeSwitcher={true}>
<div>
</div><div id="selection_lazypromise">
## lazyPromise()

```ts
function lazyPromise<T>(): {
  promise: Promise<T>;
  reject: rejectFunc;
  resolve: resolveFunc;
 }
```

Creates a lazy promise that can be resolved or rejected externally.

### Type Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Type Parameter</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`T`

</td>
<td>

The type of the value that the promise resolves to.

</td>
</tr>
</tbody>
</table>

### Returns

```ts
{
  promise: Promise<T>;
  reject: rejectFunc;
  resolve: resolveFunc;
 }
```

An object containing the promise and its resolve and reject functions.

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Name</th>
<th>Type</th>
<th>Default value</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`promise`

</td>
<td>

`Promise<T>`

</td>
<td>

&hyphen;

</td>
</tr>
<tr>
<td>

`reject`

</td>
<td>

`(reason?: any) => void`

</td>
<td>

rejectFunc

</td>
</tr>
<tr>
<td>

`resolve`

</td>
<td>

`(value: T | PromiseLike<T>) => void`

</td>
<td>

resolveFunc

</td>
</tr>
</tbody>
</table>

### Examples

```typescript
const { promise, resolve, reject } = lazyPromise<number>();

promise.then(value => {
  console.log(value); // Output: 42
});

// Resolve the promise with a value
resolve(42);
```

 *

```typescript
const { promise, resolve, reject } = lazyPromise<number>();

promise.catch(error => {
  console.error(error); // Output: Error: Something went wrong
});

// Reject the promise with an error
reject(new Error('Something went wrong'));
```

</div>
</MdxPage>
