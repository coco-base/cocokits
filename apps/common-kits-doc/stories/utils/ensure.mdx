
[//]: # (Do not edit directly)
[//]: # (Generated by the Cocokits TypeDoc custom theme on Sun Jan 19 2025)

import { Meta } from '@storybook/addon-docs';
import { MdxPage } from '@cocokits/storybook-addon-theme';

export const tocItems = [{"id":"selection_hasnotvalue","name":"hasNotValue()"},{"id":"selection_hasvalue","name":"hasValue()"},{"id":"selection_isarray","name":"isArray()"},{"id":"selection_isblob","name":"isBlob()"},{"id":"selection_isboolean","name":"isBoolean()"},{"id":"selection_isnotarray","name":"isNotArray()"},{"id":"selection_isnotblob","name":"isNotBlob()"},{"id":"selection_isnotboolean","name":"isNotBoolean()"},{"id":"selection_isnotnullish","name":"isNotNullish()"},{"id":"selection_isnotnumber","name":"isNotNumber()"},{"id":"selection_isnotstring","name":"isNotString()"},{"id":"selection_isnullish","name":"isNullish()"},{"id":"selection_isnumber","name":"isNumber()"},{"id":"selection_isnumberornan","name":"isNumberOrNaN()"},{"id":"selection_isnutobject","name":"isNutObject()"},{"id":"selection_isobject","name":"isObject()"},{"id":"selection_isstring","name":"isString()"},{"id":"selection_isstringandnotblank","name":"isStringAndNotBlank()"}];

<Meta title='Utils/Ensure'/>
<MdxPage breadcrumb='Utils' title='Ensure' tocItems={tocItems} hideThemeSwitcher={true}>
<div>
</div><div id="selection_hasnotvalue">
## hasNotValue()

```ts
function hasNotValue(value: any): boolean
```

Determines whether the provided value does not have a meaningful value.
It negates the result of `hasValue` to identify nullish, empty strings, invalid numbers, empty arrays, or empty objects.

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`value`

</td>
<td>

`any`

</td>
<td>

The value to check.

</td>
</tr>
</tbody>
</table>

### Returns

`boolean`

Returns `true` if the value is nullish, an empty string, `NaN`, or an empty object/array, otherwise `false`.

### Example

```typescript
console.log(hasNotValue(''));   // true
console.log(hasNotValue(null)); // true
console.log(hasNotValue(123));  // false
console.log(hasNotValue([]));   // true
console.log(hasNotValue({}));   // true
```

***

</div><div id="selection_hasvalue">
## hasValue()

```ts
function hasValue(value: any): boolean
```

Determines whether the provided value has a meaningful value.
It checks for non-nullish values, non-empty strings, valid numbers, blobs, non-empty arrays, and non-empty objects.

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`value`

</td>
<td>

`any`

</td>
<td>

The value to check.

</td>
</tr>
</tbody>
</table>

### Returns

`boolean`

Returns `true` if the value is not nullish, an empty string, `NaN`, or an empty object/array, otherwise `false`.

### Example

```typescript
console.log(hasValue('hello')); // true
console.log(hasValue(''));      // false
console.log(hasValue(123));     // true
console.log(hasValue(NaN));     // false
console.log(hasValue([]));      // false
console.log(hasValue({}));      // false
```

***

</div><div id="selection_isarray">
## isArray()

```ts
function isArray<T>(value: any): value is T[]
```

Checks if the provided value is an array.

### Type Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Type Parameter</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`T`

</td>
<td>

The type of elements the array is expected to contain.

</td>
</tr>
</tbody>
</table>

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`value`

</td>
<td>

`any`

</td>
<td>

The value to check if it is an array.

</td>
</tr>
</tbody>
</table>

### Returns

`value is T[]`

Returns true if the value is an array, otherwise false.

### Example

```typescript
const result = isArray([1, 2, 3]); // true
const result2 = isArray('string'); // false
```

***

</div><div id="selection_isblob">
## isBlob()

```ts
function isBlob(value: any): value is Blob
```

Determines whether the provided value is a Blob object.

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`value`

</td>
<td>

`any`

</td>
<td>

The value to check.

</td>
</tr>
</tbody>
</table>

### Returns

`value is Blob`

Returns `true` if the value is a Blob object, otherwise `false`.

### Example

```typescript
const file = new File(["content"], "example.txt", { type: "text/plain" });
const blob = new Blob(["content"], { type: "text/plain" });

console.log(isBlob(file)); // false
console.log(isBlob(blob)); // true
```

***

</div><div id="selection_isboolean">
## isBoolean()

```ts
function isBoolean(value: any): value is boolean
```

Determines whether the provided value is a boolean.

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`value`

</td>
<td>

`any`

</td>
<td>

The value to check.

</td>
</tr>
</tbody>
</table>

### Returns

`value is boolean`

Returns `true` if the value is a boolean, otherwise `false`.

### Example

```typescript
console.log(isBoolean(true)); // true
console.log(isBoolean(42));   // false
```

***

</div><div id="selection_isnotarray">
## isNotArray()

```ts
function isNotArray<T, U>(value: T): value is Exclude<T, U[]>
```

Checks if the provided value is *not* an array.

### Type Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Type Parameter</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`T`

</td>
<td>

The type of elements the array is expected to contain.

</td>
</tr>
<tr>
<td>

`U`

</td>
<td>

&hyphen;

</td>
</tr>
</tbody>
</table>

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`value`

</td>
<td>

`T`

</td>
<td>

The value to check if it is not an array.

</td>
</tr>
</tbody>
</table>

### Returns

`value is Exclude<T, U[]>`

Returns true if the value is not an array, otherwise false.

### Example

```typescript
console.log(isNotArray('string'));   // true
console.log(isNotArray([1, 2, 3])); // false
```

***

</div><div id="selection_isnotblob">
## isNotBlob()

```ts
function isNotBlob<T>(value: T): value is Exclude<T, Blob>
```

Determines whether the provided value is not a Blob object.

### Type Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Type Parameter</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`T`

</td>
<td>

The type of the value being checked.

</td>
</tr>
</tbody>
</table>

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`value`

</td>
<td>

`T`

</td>
<td>

The value to check.

</td>
</tr>
</tbody>
</table>

### Returns

`value is Exclude<T, Blob>`

Returns `true` if the value is not a Blob, otherwise `false`.

### Example

```typescript
const file = new File(["content"], "example.txt", { type: "text/plain" });
const blob = new Blob(["content"], { type: "text/plain" });

console.log(isNotBlob(file)); // true
console.log(isNotBlob(blob)); // false
```

***

</div><div id="selection_isnotboolean">
## isNotBoolean()

```ts
function isNotBoolean<T>(value: T): value is Exclude<T, boolean>
```

Determines whether the provided value is not a boolean.

### Type Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Type Parameter</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`T`

</td>
<td>

The type of the value being checked.

</td>
</tr>
</tbody>
</table>

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`value`

</td>
<td>

`T`

</td>
<td>

The value to check.

</td>
</tr>
</tbody>
</table>

### Returns

`value is Exclude<T, boolean>`

Returns `true` if the value is not a boolean, otherwise `false`.

### Example

```typescript
console.log(isNotBoolean(42));    // true
console.log(isNotBoolean(false)); // false
```

***

</div><div id="selection_isnotnullish">
## isNotNullish()

```ts
function isNotNullish<T>(value: T): value is Exclude<T, null>
```

Determines whether the provided value is not `null` or `undefined`.

### Type Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Type Parameter</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`T`

</td>
<td>

The type of the value being checked.

</td>
</tr>
</tbody>
</table>

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`value`

</td>
<td>

`T`

</td>
<td>

The value to check.

</td>
</tr>
</tbody>
</table>

### Returns

`value is Exclude<T, null>`

Returns `true` if the value is neither `null` nor `undefined`, otherwise `false`.

### Example

```typescript
console.log(isNotNullish(42));        // true
console.log(isNotNullish(null));      // false
console.log(isNotNullish(undefined)); // false
```

***

</div><div id="selection_isnotnumber">
## isNotNumber()

```ts
function isNotNumber<T>(value: T): value is Exclude<T, number>
```

Determines whether the provided value is not a `number`.

### Type Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Type Parameter</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`T`

</td>
<td>

The type of the value being checked.

</td>
</tr>
</tbody>
</table>

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`value`

</td>
<td>

`T`

</td>
<td>

The value to check.

</td>
</tr>
</tbody>
</table>

### Returns

`value is Exclude<T, number>`

Returns `true` if the value is not a `number`, otherwise `false`.

### Example

```typescript
console.log(isNotNumber(42));       // false
console.log(isNotNumber(NaN));      // false
console.log(isNotNumber('value'));  // true
```

***

</div><div id="selection_isnotstring">
## isNotString()

```ts
function isNotString<T>(value: T): value is Exclude<T, string>
```

Determines whether the provided value is not a `string`.

### Type Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Type Parameter</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`T`

</td>
<td>

The type of the value being checked.

</td>
</tr>
</tbody>
</table>

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`value`

</td>
<td>

`T`

</td>
<td>

The value to check.

</td>
</tr>
</tbody>
</table>

### Returns

`value is Exclude<T, string>`

Returns `true` if the value is not a string, otherwise `false`.

### Example

```typescript
console.log(isNotString(123));     // true
console.log(isNotString('hello')); // false
console.log(isNotString(true));    // true
```

***

</div><div id="selection_isnullish">
## isNullish()

```ts
function isNullish(value: any): value is null
```

Determines whether the provided value is `null` or `undefined`.

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`value`

</td>
<td>

`any`

</td>
<td>

The value to check.

</td>
</tr>
</tbody>
</table>

### Returns

`value is null`

Returns `true` if the value is `null` or `undefined`, otherwise `false`.

### Example

```typescript
console.log(isNullish(null));       // true
console.log(isNullish(undefined));  // true
console.log(isNullish('value'));    // false
```

***

</div><div id="selection_isnumber">
## isNumber()

```ts
function isNumber(value: any): value is number
```

Determines whether the provided value is a valid `number` (not `NaN`).

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`value`

</td>
<td>

`any`

</td>
<td>

The value to check.

</td>
</tr>
</tbody>
</table>

### Returns

`value is number`

Returns `true` if the value is a valid number and not `NaN`, otherwise `false`.

### Example

```typescript
console.log(isNumber(42));      // true
console.log(isNumber(NaN));     // false
console.log(isNumber('value')); // false
```

***

</div><div id="selection_isnumberornan">
## isNumberOrNaN()

```ts
function isNumberOrNaN(value: any): value is number
```

Determines whether the provided value is a `number` or `NaN`.

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`value`

</td>
<td>

`any`

</td>
<td>

The value to check.

</td>
</tr>
</tbody>
</table>

### Returns

`value is number`

Returns `true` if the value is a number (including `NaN`), otherwise `false`.

### Example

```typescript
console.log(isNumberOrNaN(42));      // true
console.log(isNumberOrNaN(NaN));     // true
console.log(isNumberOrNaN('value')); // false
```

***

</div><div id="selection_isnutobject">
## isNutObject()

```ts
function isNutObject<T>(value: T): value is Exclude<T, object>
```

Determines whether the provided value is not an `object` (i.e., excluding non-null objects).

### Type Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Type Parameter</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`T`

</td>
<td>

The type of the value being checked.

</td>
</tr>
</tbody>
</table>

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`value`

</td>
<td>

`T`

</td>
<td>

The value to check.

</td>
</tr>
</tbody>
</table>

### Returns

`value is Exclude<T, object>`

Returns `true` if the value is not an object, otherwise `false`.

### Example

```typescript
console.log(isNutObject(42));       // true
console.log(isNutObject(null));     // true
console.log(isNutObject({}));       // false
console.log(isNutObject('string')); // true
```

***

</div><div id="selection_isobject">
## isObject()

```ts
function isObject(value: any): value is object
```

Determines whether the provided value is an `object` (excluding `null` and arrays).

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`value`

</td>
<td>

`any`

</td>
<td>

The value to check.

</td>
</tr>
</tbody>
</table>

### Returns

`value is object`

Returns `true` if the value is a non-null object and not an array, otherwise `false`.

### Example

```typescript
console.log(isObject({}));       // true
console.log(isObject(null));     // false
console.log(isObject([]));       // false
console.log(isObject('string')); // false
```

***

</div><div id="selection_isstring">
## isString()

```ts
function isString(value: any): value is string
```

Determines whether the provided value is a `string`.

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`value`

</td>
<td>

`any`

</td>
<td>

The value to check.

</td>
</tr>
</tbody>
</table>

### Returns

`value is string`

Returns `true` if the value is a string, otherwise `false`.

### Example

```ts
// Example 1: Check if a value is a string
console.log(isString('hello'));    // Output: true
console.log(isString(123));        // Output: false
console.log(isString(null));       // Output: false
```

***

</div><div id="selection_isstringandnotblank">
## isStringAndNotBlank()

```ts
function isStringAndNotBlank(value: any): value is string
```

Determines whether the provided value is a non-blank `string`.

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`value`

</td>
<td>

`any`

</td>
<td>

The value to check.

</td>
</tr>
</tbody>
</table>

### Returns

`value is string`

Returns `true` if the value is a non-null, non-undefined string and not blank, otherwise `false`.

### Example

```
console.log(isStringAndNotBlank('hello')); // Output: true
console.log(isStringAndNotBlank('   ')); // Output: false
console.log(isStringAndNotBlank(null)); // Output: false
```

</div>
</MdxPage>
