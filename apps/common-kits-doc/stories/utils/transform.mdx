
[//]: # (Do not edit directly)
[//]: # (Generated by the Cocokits TypeDoc custom theme on Wed Feb 12 2025)

import { Meta } from '@storybook/addon-docs';
import { MdxPage } from '@cocokits/storybook-addon-theme';

export const tocItems = [{"id":"selection_toarray","name":"toArray()"},{"id":"selection_tobooleanorpresent","name":"toBooleanOrPresent()"},{"id":"selection_tonumber","name":"toNumber()"}];

<Meta title='Utils/Transform'/>
<MdxPage breadcrumb='Utils' title='Transform' tocItems={tocItems} hideThemeSwitcher={true}>
<div>
</div><div id="selection_toarray">
## toArray()

```ts
function toArray<T>(value: T | T[]): T[]
```

Converts a single value, an array of values, or a nullish value into an array.

This function ensures that the returned value is always an array. If the input is already an array,
it returns the array as-is. If the input is a single value, it wraps that value in an array.
If the input is `null` or `undefined`, it returns an empty array.

### Type Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Type Parameter</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`T`

</td>
<td>

The type of the input value.

</td>
</tr>
</tbody>
</table>

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`value`

</td>
<td>

`T | T[]`

</td>
<td>

The value to be converted into an array. It can be a single value, an array, or `null`/`undefined`.

</td>
</tr>
</tbody>
</table>

### Returns

`T[]`

An array containing the input value, the input array as-is, or an empty array if the input is nullish.

```typescript
console.log(toArray(42)); // Outputs: [42]
const array = toArray([1, 2, 3]); // Outputs: [1, 2, 3]
const array = toArray(null); // Outputs: []
```

***

</div><div id="selection_tobooleanorpresent">
## toBooleanOrPresent()

```ts
function toBooleanOrPresent(value: any): boolean
```

Converts a potentially undefined, null, or string value to a boolean.
Specifically, it treats the attribute presence (like `disabled` with no value)
as `true`, and interprets the string "false" explicitly as `false`.
Other non-null/undefined values are treated as `true`.

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`value`

</td>
<td>

`any`

</td>
<td>

The value to convert, can be of any type.

</td>
</tr>
</tbody>
</table>

### Returns

`boolean`

The boolean result of the conversion.

### Examples

```typescript
toBooleanOrPresent('false'); // false;
toBooleanOrPresent('FALSE'); // false;
toBooleanOrPresent(null); // false;
toBooleanOrPresent(''); // true;
toBooleanOrPresent('true'); // true;
toBooleanOrPresent('yes'); // true;
```

Use it in Angular
```typescript
disabled = input(undefined, {transform: toBooleanOrPresent})
// or
@Input({transform: toBooleanOrPresent}) disabled: boolean;
```
```html
<button disabled>Click me</button> <!-- true -->
<button [disabled]="true">Click me</button> <!-- true -->
<button [disabled]="yes">Click me</button> <!-- true -->
<button>Click me</button> <!-- false -->
<button [disabled]="false">Click me</button> <!-- false -->
<button [disabled]="FALSE">Click me</button> <!-- false -->
```

***

</div><div id="selection_tonumber">
## toNumber()

```ts
function toNumber(value: any): number
```

Converts a potentially undefined, null, or string value to a numeric representation.
If the provided value cannot be parsed as a valid number, an error is thrown.

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`value`

</td>
<td>

`any`

</td>
<td>

The value to convert, can be of any type.

</td>
</tr>
</tbody>
</table>

### Returns

`number`

The numeric result of the conversion.

### Examples

```typescript
toNumber(42); // 42
toNumber('42'); // 42
toNumber('3.14'); // 3.14
toNumber('007'); // 7
toNumber(''); // 0
```

Use it in an Angular component:
```typescript
rating = input('4.5', { transform: toNumber });
// or
@Input({ transform: toNumber }) rating: number;
```
```html
<input value="3" />
<input [value]="'3'" />
```

</div>
</MdxPage>
