
[//]: # (Do not edit directly)
[//]: # (Generated by the Cocokits TypeDoc custom theme)

import { Meta } from '@storybook/addon-docs';
import { MdxPage } from '@cocokits/storybook-addon-theme';

export const tocItems = [{"id":"selection_useondestroy","name":"useOnDestroy()"}];

<Meta title='Utils/Use On Destroy'/>
<MdxPage breadcrumb='Utils' title='Use On Destroy' tocItems={tocItems} hideThemeSwitcher={true}>
<div>
</div><div id="selection_useondestroy">
## useOnDestroy()

```ts
function useOnDestroy(callback: () => void): void
```

A custom hook that runs a callback function when the component is destroyed.
This is useful for performing cleanup tasks such as removing event listeners or cancelling subscriptions.
It's same as `useEffect` with an empty dependency array, but specifically intended for cleanup actions.

### Parameters

<div className="arg-type-table-wrapper">
<table className="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th className="description-column">Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`callback`

</td>
<td>

`() => void`

</td>
<td>

The function to run on component destruction.

</td>
</tr>
</tbody>
</table>
</div>

### Returns

`void`

### Example

```ts
// Usage example:
function MyComponent() {
  useOnDestroy(() => {
    console.log('Component destroyed');
  });

  return <div>My Component</div>;
}
```

</div>
</MdxPage>
