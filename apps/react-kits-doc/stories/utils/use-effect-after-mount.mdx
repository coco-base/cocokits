
[//]: # (Do not edit directly)
[//]: # (Generated by the Cocokits TypeDoc custom theme)

import { Meta } from '@storybook/addon-docs';
import { MdxPage } from '@cocokits/storybook-addon-theme';

export const tocItems = [{"id":"selection_useeffectaftermount","name":"useEffectAfterMount()"}];

<Meta title='Utils/Use Effect After Mount'/>
<MdxPage breadcrumb='Utils' title='Use Effect After Mount' tocItems={tocItems} hideThemeSwitcher={true}>
<div>
</div><div id="selection_useeffectaftermount">
## useEffectAfterMount()

```ts
function useEffectAfterMount(effectCallback: EffectCallback, deps?: DependencyList): void
```

A custom hook that runs an effect callback only after the initial mount.
Unlike useEffect, this hook skips executing the callback on the first render.

### Parameters

<div className="arg-type-table-wrapper">
<table className="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th className="description-column">Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`effectCallback`

</td>
<td>

`EffectCallback`

</td>
<td>

The effect function to run after mount and when dependencies change

</td>
</tr>
<tr>
<td>

`deps`?

</td>
<td>

`DependencyList`

</td>
<td>

Optional dependencies array that controls when the effect runs

</td>
</tr>
</tbody>
</table>
</div>

### Returns

`void`

### Example

```ts
// Usage example:
function Counter({ value }) {
  const [count, setCount] = useState(value);

  // This will only run when 'value' changes, not on initial mount
  useEffectAfterMount(() => {
    setCount(value);
  }, [value]);

  return <div>{count}</div>;
}
```

</div>
</MdxPage>
