
[//]: # (Do not edit directly)
[//]: # (Generated by the Cocokits TypeDoc custom theme)

import { Meta } from '@storybook/addon-docs';
import { MdxPage } from '@cocokits/storybook-addon-theme';

export const tocItems = [{"id":"selection_useoninit","name":"useOnInit()"}];

<Meta title='Utils/Use On Init'/>
<MdxPage breadcrumb='Utils' title='Use On Init' tocItems={tocItems} hideThemeSwitcher={true}>
<div>
</div><div id="selection_useoninit">
## useOnInit()

```ts
function useOnInit(callback: () => void): void
```

A custom hook that runs a callback function only once when the component is initialized.
This is useful for performing setup tasks that should only happen once, such as initializing state or fetching data.

### Parameters

<div class="arg-type-table-wrapper">
<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th class="description-column">Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`callback`

</td>
<td>

`() => void`

</td>
<td>

The function to run on initialization.

</td>
</tr>
</tbody>
</table>
</div>

### Returns

`void`

### Example

```ts
// Usage example:
function MyComponent() {
  useOnInit(() => {
    console.log('Component initialized');
  });

  return <div>My Component</div>;
}
```

</div>
</MdxPage>
