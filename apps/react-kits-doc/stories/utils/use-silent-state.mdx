
[//]: # (Do not edit directly)
[//]: # (Generated by the Cocokits TypeDoc custom theme on Sun Mar 09 2025)

import { Meta } from '@storybook/addon-docs';
import { MdxPage } from '@cocokits/storybook-addon-theme';

export const tocItems = [{"id":"selection_usesilentstate","name":"useSilentState()"}];

<Meta title='Utils/Use-silent-state'/>
<MdxPage breadcrumb='Utils' title='Use-silent-state' tocItems={tocItems} hideThemeSwitcher={true}>
<div>
</div><div id="selection_usesilentstate">
## useSilentState()

```ts
function useSilentState<T>(initValue?: T): readonly [null | T, (value: null | T) => void]
```

A custom hook that manages state without triggering re-renders when the state changes.
This is useful for storing values that need to persist across renders but do not require
the component to update when the value changes.

### Type Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Type Parameter</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`T`

</td>
<td>

The type of the stored value.

</td>
</tr>
</tbody>
</table>

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`initValue`?

</td>
<td>

`T`

</td>
<td>

The initial value for the state. If not provided, the initial value will be `null`.

</td>
</tr>
</tbody>
</table>

### Returns

`readonly [null | T, (value: null | T) => void]`

- A tuple containing the current value and a function to update it.

### Example

```ts
// Example usage in a functional component
function MyComponent() {
  const [value, setValue] = useSilentState<string>("initial");

  useEffect(() => {
    // Update the value without causing a re-render
    setValue("updated");
  }, []);

  return (
    <div>
      <p>Current value: {value}</p>
      <button onClick={() => setValue("new value")}>Update Value</button>
    </div>
  );
}
```

</div>
</MdxPage>
