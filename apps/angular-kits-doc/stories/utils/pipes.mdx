
[//]: # (Do not edit directly)
[//]: # (Generated by the Cocokits TypeDoc custom theme on Wed Mar 12 2025)

import { Meta } from '@storybook/addon-docs';
import { MdxPage } from '@cocokits/storybook-addon-theme';

export const tocItems = [{"id":"selection_objtokeyspipe","name":"ObjToKeysPipe"},{"id":"selection_trusthtmlpipe","name":"TrustHtmlPipe"}];

<Meta title='Utils/Pipes'/>
<MdxPage breadcrumb='Utils' title='Pipes' tocItems={tocItems} hideThemeSwitcher={true}>
<div>
</div><div id="selection_objtokeyspipe">
## ObjToKeysPipe

A pipe that transforms an object into an array of its keys.

### Example

Use this pipe to list object keys in a template.
```html
<ul>
  <li *ngFor="let key of myObject | toObjKeys">{{ key }}</li>
</ul>
```

```typescript
const myObject = { foo: 1, bar: 2 };`
```

### Implements

- `PipeTransform`

### Methods

#### transform()

```ts
transform<T>(value: T): keyof T[]
```

##### Type Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Type Parameter</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`T`

</td>
</tr>
</tbody>
</table>

##### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`value`

</td>
<td>

`T`

</td>
</tr>
</tbody>
</table>

##### Returns

`keyof T[]`

##### Implementation of

`PipeTransform.transform`

***

</div><div id="selection_trusthtmlpipe">
## TrustHtmlPipe

A pipe that sanitizes a string of HTML to bypass Angular's built-in security and display trusted HTML content.
It uses Angular's `DomSanitizer` service to safely allow HTML content to be rendered.

### Example

In this example, the HTML content of `someHtmlString` will be rendered as trusted HTML, bypassing Angular's security checks.
```html
<div [innerHTML]="someHtmlString | trustHtml"></div>
```

### Implements

- `PipeTransform`

</div>
</MdxPage>
