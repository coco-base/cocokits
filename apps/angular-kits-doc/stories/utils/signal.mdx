
[//]: # (Do not edit directly)
[//]: # (Generated by the Cocokits TypeDoc custom theme on Sat Mar 22 2025)

import { Meta } from '@storybook/addon-docs';
import { MdxPage } from '@cocokits/storybook-addon-theme';

export const tocItems = [{"id":"selection_fromcontroloption","name":"FromControlOption"},{"id":"selection_fromattrbyname","name":"fromAttrByName()"},{"id":"selection_fromattrbynametoboolean","name":"fromAttrByNameToBoolean()"},{"id":"selection_fromattrwithprefix","name":"fromAttrWithPrefix()"},{"id":"selection_fromcontrol","name":"fromControl()"},{"id":"selection_todeepsignal","name":"toDeepSignal()"}];

<Meta title='Utils/Signal'/>
<MdxPage breadcrumb='Utils' title='Signal' tocItems={tocItems} hideThemeSwitcher={true}>
<div>
</div><div id="selection_fromcontroloption">
## FromControlOption

Options for configuring the behavior of the `fromControl` function, including
a reference for managing component lifecycle and clean-up.

### Properties

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Property</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`destroyRef?`

</td>
<td>

`DestroyRef`

</td>
<td>

Optional angular `DestroyRef` reference.

</td>
</tr>
</tbody>
</table>

***

</div><div id="selection_fromattrbyname">
## fromAttrByName()

```ts
function fromAttrByName<T>(name: string): InputSignal<T>
```

Creates a signal that tracks changes to a specified attribute of an HTML element.

This function creates a reactive signal bound to the value of the attribute `name` on the
host element. The signal updates automatically when the attribute changes.

### Type Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Type Parameter</th>
<th>Default type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`T`

</td>
<td>

`unknown`

</td>
<td>

The type of the attribute's value.

</td>
</tr>
</tbody>
</table>

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`name`

</td>
<td>

`string`

</td>
<td>

The name of the attribute to observe.

</td>
</tr>
</tbody>
</table>

### Returns

`InputSignal<T>`

A readonly signal that tracks the attribute value.

### Example

```typescript
const attrSignal = fromAttrByName<string>('data-example');
console.log(attrSignal()); // Logs the current value of the 'data-example' attribute
```

***

</div><div id="selection_fromattrbynametoboolean">
## fromAttrByNameToBoolean()

```ts
function fromAttrByNameToBoolean(name: string): InputSignal<boolean>
```

Creates a signal that tracks the existence and value of a boolean attribute on an HTML element.

This function is useful for attributes like `disabled` or `checked`, which may exist in the DOM
without an explicit value. If the attribute is present without a value, the signal returns `true`.
If the attribute has a value, it returns a boolean based on its value.

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`name`

</td>
<td>

`string`

</td>
<td>

The name of the attribute to observe and convert to a boolean.

</td>
</tr>
</tbody>
</table>

### Returns

`InputSignal<boolean>`

A readonly signal that represents the boolean state of the attribute.

### Example

```typescript
const booleanAttrSignal = fromAttrByNameToBoolean('disabled');
console.log(booleanAttrSignal()); // Logs `true` if the attribute is present, `false` otherwise
```

***

</div><div id="selection_fromattrwithprefix">
## fromAttrWithPrefix()

```ts
function fromAttrWithPrefix<T>(prefix: string): InputSignal<T>
```

Creates a signal that tracks changes to multiple attributes with a specified prefix.

This function monitors all attributes on the host element that start with the given `prefix`
and returns an object where each attribute (minus the prefix) is a key, and its value is
the attribute's value. The signal updates automatically when any of the matching attributes change.

### Type Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Type Parameter</th>
<th>Default type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`T`

</td>
<td>

`Record<string, unknown>`

</td>
<td>

The type of the result object containing the attribute key-value pairs.

</td>
</tr>
</tbody>
</table>

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`prefix`

</td>
<td>

`string`

</td>
<td>

The prefix of the attributes to observe.

</td>
</tr>
</tbody>
</table>

### Returns

`InputSignal<T>`

A readonly signal that represents an object with the matching attributes' key-value pairs.

### Example

```typescript
const prefixedAttrsSignal = fromAttrWithPrefix('data-');
console.log(prefixedAttrsSignal()); // Logs an object with all 'data-' prefixed attributes
```

***

</div><div id="selection_fromcontrol">
## fromControl()

```ts
function fromControl<T>(control: AbstractControl<any, any>, options: FromControlOption): AbstractControlSignalStates<T>
```

Converts an `AbstractControl` into a set of reactive signals that represent its state and status.
The function returns various control states as writable signals, allowing you to reactively track
the form control's state like value, validity, and interaction statuses.

The state is updated automatically whenever the control emits changes.

### Type Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Type Parameter</th>
<th>Default type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`T`

</td>
<td>

`unknown`

</td>
<td>

The type of the control's value.

</td>
</tr>
</tbody>
</table>

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`control`

</td>
<td>

`AbstractControl<any, any>`

</td>
<td>

The `AbstractControl` instance to convert into signals.

</td>
</tr>
<tr>
<td>

`options`

</td>
<td>

`FromControlOption`

</td>
<td>

Additional configuration options like lifecycle management.

</td>
</tr>
</tbody>
</table>

### Returns

`AbstractControlSignalStates<T>`

An object containing writable signals for the control's state and status.

### Example

```typescript
const control = new FormControl('test');
const signals = fromControl(control);

console.log(signals.value());  // Logs the current control value
console.log(signals.valid());  // Logs the control's validity
```

***

</div><div id="selection_todeepsignal">
## toDeepSignal()

```ts
function toDeepSignal<T>(signal: Signal<T>): DeepSignal<T>
```

Converts a given signal into a deep signal, allowing the tracking of nested object properties as signals.

### Type Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Type Parameter</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`T`

</td>
<td>

The type of the value held by the signal.

</td>
</tr>
</tbody>
</table>

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`signal`

</td>
<td>

`Signal<T>`

</td>
<td>

The signal to convert into a deep signal.

</td>
</tr>
</tbody>
</table>

### Returns

`DeepSignal<T>`

Returns a `DeepSignal` that proxies deeply nested object properties, allowing them to be signals.

### Example

```typescript
const signal = signal({ name: 'John', address: { city: 'Vienna' } });
const deepSignal = toDeepSignal(signal);

// Access deeply nested properties
console.log(deepSignal().address.city); // Vienna
console.log(deepSignal.address().city); // Vienna
console.log(deepSignal.address.city()); // Vienna
```

</div>
</MdxPage>
