{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "ScssBuilder executor",
  "description": "",
  "type": "object",
  "properties": {
    "outputPath": {
      "type": "string",
      "description": "The output path of the generated files."
    },
    "sourceMap": {
      "type": "boolean",
      "description": "Flag to determine if source maps should be generated.",
      "default": false
    },
    "css": {
      "type": "array",
      "description": "Array of paths for CSS files.",
      "items": {
        "type": "string"
      }
    },
    "min": {
      "type": "array",
      "description": "Optional array of paths for minified files.",
      "items": {
        "type": "string"
      },
      "default": []
    }
  },
  "required": ["outputPath", "css"]
}
