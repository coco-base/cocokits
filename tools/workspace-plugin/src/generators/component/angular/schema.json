{
  "$schema": "https://json-schema.org/schema",
  "$id": "AngularComponent",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    },
    "project": {
      "type": "string",
      "description": "The project where the component will be located.",
      "x-prompt": "Which project will this component be located in?",
      "x-dropdown": "projects"
    },
    "subDirectory": {
      "type": "string",
      "description": "Sub-folder inside the 'lib' directory of target project where the component will be created.",
      "x-prompt": "What is the sub-folder 'lib' directory of target project that should contain the new component?",
      "default": "./"
    },
    "story": {
      "type": "boolean",
      "description": "Whether generate story of the component for storybook",
      "x-prompt": "Is the component has story for storybook?",
      "default": true
    }
  },
  "required": ["name", "project"]
}
