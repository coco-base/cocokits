{
  "overrides": [
    {
      "files": ["*.ts", "*.tsx"],
      "extends": ["./eslint.base.json"],
      "rules": {
        "react-hooks/exhaustive-deps": "off",
        "react/jsx-no-useless-fragment": "off",
        "max-lines-per-function": ["error", { "max": 140, "skipComments": true }]
      }
    }
  ]
}
