
[//]: # (Do not edit directly)
[//]: # (Generated by the Cocokits TypeDoc custom theme)

import { Meta } from '@storybook/addon-docs';
import { MdxPage } from '@cocokits/storybook-addon-theme';

export const tocItems = [{"id":"selection_deepcomparator","name":"deepComparator()"},{"id":"selection_simplecomparator","name":"simpleComparator()"}];

<Meta title='Utils/Comparator'/>
<MdxPage breadcrumb='Utils' title='Comparator' tocItems={tocItems} hideThemeSwitcher={true}>
<div>
</div><div id="selection_deepcomparator">
## deepComparator()

```ts
function deepComparator<T>(previous: T, current: T): boolean
```

Compares two objects for deep equality.

### Type Parameters

<div className="arg-type-table-wrapper">
<table className="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Type Parameter</th>
<th className="description-column">Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`T` *extends* `Record<string | number | symbol, any>`

</td>
<td>

The type of the objects being compared.

</td>
</tr>
</tbody>
</table>
</div>

### Parameters

<div className="arg-type-table-wrapper">
<table className="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th className="description-column">Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`previous`

</td>
<td>

`T`

</td>
<td>

The previous object.

</td>
</tr>
<tr>
<td>

`current`

</td>
<td>

`T`

</td>
<td>

The current object.

</td>
</tr>
</tbody>
</table>
</div>

### Returns

`boolean`

True if both objects are deeply equal, false otherwise.

### Example

```ts
const obj1 = { a: 1, b: { c: 2 } };
const obj2 = { a: 1, b: { c: 2 } };
const obj3 = { a: 1, b: { c: 3 } };

console.log(deepComparator(obj1, obj2)); // true
console.log(deepComparator(obj1, obj3)); // false
```

***

</div><div id="selection_simplecomparator">
## simpleComparator()

```ts
function simpleComparator<T>(previous: T, current: T): boolean
```

Compares two objects for shallow equality.

### Type Parameters

<div className="arg-type-table-wrapper">
<table className="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Type Parameter</th>
<th className="description-column">Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`T` *extends* `Record<string | number | symbol, any>`

</td>
<td>

The type of the objects being compared.

</td>
</tr>
</tbody>
</table>
</div>

### Parameters

<div className="arg-type-table-wrapper">
<table className="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th className="description-column">Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`previous`

</td>
<td>

`T`

</td>
<td>

The previous object.

</td>
</tr>
<tr>
<td>

`current`

</td>
<td>

`T`

</td>
<td>

The current object.

</td>
</tr>
</tbody>
</table>
</div>

### Returns

`boolean`

True if both objects are shallowly equal, false otherwise.

### Example

```ts
const obj1 = { a: 1, b: 2 };
const obj2 = { a: 1, b: 2 };
const obj3 = { a: 1, b: 3 };

console.log(simpleComparator(obj1, obj2)); // true
console.log(simpleComparator(obj1, obj3)); // false
```

</div>
</MdxPage>
