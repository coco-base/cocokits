
[//]: # (Do not edit directly)
[//]: # (Generated by the Cocokits TypeDoc custom theme)

import { Meta } from '@storybook/addon-docs';
import { MdxPage } from '@cocokits/storybook-addon-theme';

export const tocItems = [{"id":"selection_elementanchorpoint","name":"ElementAnchorPoint"},{"id":"selection_getelementanchorposition","name":"getElementAnchorPosition()"}];

<Meta title='Utils/DOM'/>
<MdxPage breadcrumb='Utils' title='DOM' tocItems={tocItems} hideThemeSwitcher={true}>
<div>
</div><div id="selection_elementanchorpoint">
## ElementAnchorPoint

Defines anchor point positions for element positioning.

These anchor points determine how one element should be positioned
relative to another element.

### Enumeration Members

<div className="arg-type-table-wrapper">
<table className="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Enumeration Member</th>
<th>Value</th>
<th className="description-column">Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`BottomLeft`

</td>
<td>

`"BottomLeft"`

</td>
<td>

Position the element at the bottom-left corner of the source

</td>
</tr>
<tr>
<td>

`BottomRight`

</td>
<td>

`"BottomRight"`

</td>
<td>

Position the element at the bottom-right corner of the source

</td>
</tr>
<tr>
<td>

`TopLeft`

</td>
<td>

`"TopLeft"`

</td>
<td>

Position the element at the top-left corner of the source

</td>
</tr>
<tr>
<td>

`TopRight`

</td>
<td>

`"TopRight"`

</td>
<td>

Position the element at the top-right corner of the source

</td>
</tr>
</tbody>
</table>
</div>

***

</div><div id="selection_getelementanchorposition">
## getElementAnchorPosition()

```ts
function getElementAnchorPosition(
   targetElement: HTMLElement, 
   sourceElement: HTMLElement, 
   anchorPoint: ElementAnchorPoint): {
  x: number;
  y: number;
 }
```

Calculates the position coordinates needed to place a target element
at a specified anchor point relative to a source element.

### Parameters

<div className="arg-type-table-wrapper">
<table className="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th className="description-column">Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`targetElement`

</td>
<td>

`HTMLElement`

</td>
<td>

The element to be positioned

</td>
</tr>
<tr>
<td>

`sourceElement`

</td>
<td>

`HTMLElement`

</td>
<td>

The reference element to position against

</td>
</tr>
<tr>
<td>

`anchorPoint`

</td>
<td>

`ElementAnchorPoint`

</td>
<td>

The desired anchor point position

</td>
</tr>
</tbody>
</table>
</div>

### Returns

```ts
{
  x: number;
  y: number;
 }
```

Coordinates representing the position offset

<div className="arg-type-table-wrapper">
<table className="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Name</th>
<th>Type</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`x`

</td>
<td>

`number`

</td>
</tr>
<tr>
<td>

`y`

</td>
<td>

`number`

</td>
</tr>
</tbody>
</table>
</div>

### Example

```ts
// Position a dropdown menu below a button
const button = document.getElementById('dropdown-button');
const menu = document.getElementById('dropdown-menu');

const position = getElementAnchorPosition(
  menu,
  button,
  ElementAnchorPoint.BottomLeft
);

menu.style.transform = `translate(${position.x}px, ${position.y}px)`;
```

</div>
</MdxPage>
