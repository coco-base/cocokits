---

# Override Styles
All selectors from core and theme packages use the `:where()` pseudo-class, ensuring 0 [specificity](https://developer.mozilla.org/en-US/docs/Web/CSS/Specificity).
This allows easy style overrides without needing `!important` or deeply nested selectors.


