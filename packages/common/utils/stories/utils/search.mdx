
[//]: # (Do not edit directly)
[//]: # (Generated by the Cocokits TypeDoc custom theme on Tue Nov 19 2024 19:05:28 GMT+0100)

import { Meta } from '@storybook/addon-docs';
import { MdxDocPage } from '../../../../../packages/internal/storybook-theme-switcher/src';

export const tocItems = [{"id":"fuzzysearch","name":"fuzzysearch()"}];

<Meta title='Utils/Search'/>
<MdxDocPage groupTitle='Utils' toc={tocItems} utilsPage={true} hideCckThemeSwitcher={true}>
# Search

## fuzzysearch()

```ts
function fuzzysearch(needle: string, haystack: string): boolean
```

Performs a fuzzy search to determine if the `needle` string is contained within the `haystack` string,
in the same order, but not necessarily consecutively.

This function is a lightweight and efficient implementation of fuzzy string matching. It checks
whether all characters in the `needle` are present in the `haystack`, in the same order.

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`needle`

</td>
<td>

`string`

</td>
<td>

The smaller string to search for within the `haystack`.

</td>
</tr>
<tr>
<td>

`haystack`

</td>
<td>

`string`

</td>
<td>

The larger string in which to search for the `needle`.

</td>
</tr>
</tbody>
</table>

### Returns

`boolean`

`true` if the `needle` is found in the `haystack` in the correct order, otherwise `false`.

### Example

```typescript
fuzzysearch('car', 'cartwheel'); // Returns true
fuzzysearch('cwhl', 'cartwheel'); // Returns true
fuzzysearch('cwhee', 'cartwheel'); // Returns true
fuzzysearch('dog', 'cartwheel'); // Returns false
```

</MdxDocPage>
