
[//]: # (Do not edit directly)
[//]: # (Generated by the Cocokits TypeDoc custom theme on Thu Dec 26 2024 16:11:10 GMT+0100)

import { Meta } from '@storybook/addon-docs';
import { MdxPage } from '../../../../../packages/internal/storybook-addon-theme/src/lib/features/mdx-page/mdx-page';

export const tocItems = [{"id":"tocamelcase","name":"toCamelCase()"},{"id":"tokebabcase","name":"toKebabCase()"},{"id":"topascalcase","name":"toPascalCase()"},{"id":"tosnakecase","name":"toSnakeCase()"},{"id":"totitlecase","name":"toTitleCase()"}];

<Meta title='Utils/String'/>
<MdxPage breadcrumb='Utils' title='String' tocItems={tocItems} hideThemeSwitcher={true}>

## toCamelCase()

```ts
function toCamelCase(str: string): string
```

Converts a string to camelCase format by lowercasing the first word
and capitalizing the first letter of subsequent words.

Splits the original string on whitespace, underscores, or dashes, then
transforms each part accordingly before concatenating them into a single
camelCase string.

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`str`

</td>
<td>

`string`

</td>
<td>

The string to convert to camelCase.

</td>
</tr>
</tbody>
</table>

### Returns

`string`

The camelCase version of the input string.

### Example

```ts
console.log(toCamelCase("hello world"));
// Output: "helloWorld"

console.log(toCamelCase("some-string_example"));
// Output: "someStringExample"
```

***

## toKebabCase()

```ts
function toKebabCase(str: string): string
```

Converts a string to kebab-case format by lowercasing all letters and joining words with dashes.

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`str`

</td>
<td>

`string`

</td>
<td>

The string to convert to kebab-case.

</td>
</tr>
</tbody>
</table>

### Returns

`string`

The kebab-case version of the input string.

### Example

```ts
console.log(toKebabCase("Hello World"));
// Output: "hello-world"
```

***

## toPascalCase()

```ts
function toPascalCase(str: string): string
```

Converts a string to PascalCase format by capitalizing the first letter of each word.

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`str`

</td>
<td>

`string`

</td>
<td>

The string to convert to PascalCase.

</td>
</tr>
</tbody>
</table>

### Returns

`string`

The PascalCase version of the input string.

### Example

```ts
console.log(toPascalCase("hello world"));
// Output: "HelloWorld"
```

***

## toSnakeCase()

```ts
function toSnakeCase(str: string): string
```

Converts a string to snake_case format by lowercasing all letters and joining words with underscores.

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`str`

</td>
<td>

`string`

</td>
<td>

The string to convert to snake_case.

</td>
</tr>
</tbody>
</table>

### Returns

`string`

The snake_case version of the input string.

### Example

```ts
console.log(toSnakeCase("Hello World"));
// Output: "hello_world"
```

***

## toTitleCase()

```ts
function toTitleCase(str: string): string
```

Converts a string to Title Case.

### Parameters

<table class="arg-type-table arg-type-table--full-width">
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`str`

</td>
<td>

`string`

</td>
<td>

The string to convert.

</td>
</tr>
</tbody>
</table>

### Returns

`string`

The Title Case version of the input string.

### Example

```ts
console.log(toTitleCase("hello world"));
// Output: "Hello World"
```

</MdxPage>
